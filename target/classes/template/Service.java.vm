/**
 * Copyright (c) 2020 金电联行 All rights reserved.
 *
 * 3Golden Authorized Use
 *
 * 版权所有，侵权必究！
 */

package ${package}.service;

import ${main}.commons.mybatis.service.CrudService;
import ${main}.commons.tools.page.PageData;
import ${package}.dto.${className}DTO;
import ${package}.entity.${className}Entity;

import java.util.List;
import java.util.Map;

/**
 * ${commentsService}
 *
 * @author ${author} ${email}
 * @since ${version} ${date}
 */
public interface ${className}Service extends CrudService<${className}Entity, ${className}DTO> {
    /**
     * 查询分页-人工SQL
     * @param params 查询条件
     * @return PageData<${className}DTO>
     */
    PageData<${className}DTO> queryPageBySql(Map<String, Object> params);
    /**
     * 查询数据列表
     * @param params 查询条件
     * @return List<${className}DTO>
     */
    List<${className}DTO> ${variableClassName}List(Map<String, Object> params);
}